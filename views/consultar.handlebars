<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tela de Consulta</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
    <div class="container">
        <h1>Tela de Consulta</h1>
        <table class="table">
            <thead>
                 <tr>
                    <td class="bg-primary text-white text-center" scope="col">Nome</td>
                    <td class="bg-primary text-white text-center" scope="col">Endereço</td>
                    <td class="bg-primary text-white text-center" scope="col">Bairro</td>
                    <td class="bg-primary text-white text-center" scope="col">CEP</td>
                    <td class="bg-primary text-white text-center" scope="col">Cidade</td>
                    <td class="bg-primary text-white text-center" scope="col">Estado</td>
                    <td class="bg-primary text-white text-center" scope="col"></td>
                    <td class="bg-primary text-white text-center" scope="col"></td>
                    <td class="bg-primary text-white text-center" scope="col"></td>

                </tr>
            </thead>
            <tbody>
                {{#each post}}
                    {{#dataValues}}
                        <tr>
                            <td class="text-center">{{nome}}</td>
                            <td class="text-center">{{endereco}}</td>
                            <td class="text-center">{{bairro}}</td>
                            <td class="text-center">{{cep}}</td>
                            <td class="text-center">{{cidade}}</td>
                            <td class="text-center">{{estado}}</td>
                            <td><a type="button" class="btn btn-warning" href="/editar/{{id}}">Editar</a></td>
                            <td><a type="reset" class="btn btn-danger" href="/excluir/{{id}}">Excluir</a></td>
                        </tr>
                  {{/dataValues}}
                {{/each}}  
            </tbody>
        </table>
    </div>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
